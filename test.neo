load math2

var x:int -> 30
echo math2.add(20, x)
echo x

__main()::None->
	echo "390"

__fib(n:int)::int->
    if n <= 1:
        return n
    else:
        return fib(n-1) + fib(n-2)

var e:int -> fib(20) 

echo e
